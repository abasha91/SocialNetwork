package com.example.socialnetworking.service;

import org.springframework.stereotype.Service;

@Service
public class FollowerServiceImp implements FollowerService {

    @Override
    public void addFollower(int followerID) {
        
        // 1. check if followerID is valid

        // 2. check if the followerID is not listed already for the logged in user

        // 3. insert to the database

    }
    
}
